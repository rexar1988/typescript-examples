import { StepperLabelDirective } from './stepper-label.directive';

describe('StepLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperLabelDirective();
    expect(directive).toBeTruthy();
  });
});
