import { StepperNextDirective } from './stepper-next.directive';

describe('StepNextDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperNextDirective();
    expect(directive).toBeTruthy();
  });
});
