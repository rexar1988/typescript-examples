import { StepperPrevDirective } from './stepper-prev.directive';

describe('StepperPrevDirective', () => {
  it('should create an instance', () => {
    const directive = new StepperPrevDirective();
    expect(directive).toBeTruthy();
  });
});
